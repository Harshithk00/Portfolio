<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Email Status</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            background-color: #1a1a1a;
            color: #e1e1e1;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            line-height: 1.6;
        }

        .container {
            max-width: 600px;
            padding: 2rem;
            text-align: center;
            background-color: #242424;
            border-radius: 12px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.3);
            margin: 1rem;
        }

        .icon {
            font-size: 4rem;
            margin-bottom: 1rem;
        }

        .success {
            color: #4caf50;
        }

        .error {
            color: #f44336;
        }

        h1 {
            font-size: 1.8rem;
            margin-bottom: 1rem;
            color: #fff;
        }

        .message {
            font-size: 1.1rem;
            margin-bottom: 2rem;
            color: #b3b3b3;
        }

        .button {
            display: inline-block;
            padding: 0.8rem 1.6rem;
            background-color: #2196f3;
            color: white;
            text-decoration: none;
            border-radius: 6px;
            transition: background-color 0.2s;
        }

        .button:hover {
            background-color: #1976d2;
        }

        @media (max-width: 480px) {
            .container {
                padding: 1.5rem;
            }

            h1 {
                font-size: 1.5rem;
            }

            .message {
                font-size: 1rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <% if (success) { %>
            <div class="icon success">✓</div>
            <h1>Email Sent Successfully</h1>
            <p class="message">Your email has been sent successfully. We'll get back to you soon!</p>
        <% } else { %>
            <div class="icon error">✕</div>
            <h1>Email Not Sent</h1>
            <p class="message">Sorry, there was an error sending your email. Please try again later.</p>
        <% } %>
        <a href="/" class="button">Back to Home</a>
    </div>
</body>
</html>